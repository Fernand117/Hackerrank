using System;

namespace Hackerrank.Algorithms
{
    public class SelectionSort
    {
        public static void GetAllAcounts()
        {
            string nombre  = "";
            string usuario = "";

            Console.Write("Bienvenido al sistema principal: ");
            string cmd = Console.ReadLine();
            string cmdInstructions = Console.ReadLine();

            if (cmd == "whoami")
            {
                Console.Write("This command is blocked.");
            }
        }
    }
}
